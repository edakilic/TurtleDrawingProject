turtle_instance = turte.Turtle()
num_sides = ###
angle = 360.0 / num_sides
side_length = ###

for i in range(num_sides):
    turtle_instance.right(angle)
    turtle_instance.forward(side_length)

turtle.done()